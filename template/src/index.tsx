import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <div>built with cra-template-xs</div>
  </React.StrictMode>,
  document.getElementById('root')
);
